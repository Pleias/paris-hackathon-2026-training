# Use PyTorch nightly index as extra (not primary) so PyPI packages still resolve
--extra-index-url https://download.pytorch.org/whl/nightly/cu130
torch>=2.7.0.dev0

# Megatron-Core for tensor parallelism primitives
megatron-core>=0.9.0

# Transformer Engine for FP8 on Blackwell B300

# Numerics / data
numpy>=1.26.0

# Testing
pytest>=8.0.0
